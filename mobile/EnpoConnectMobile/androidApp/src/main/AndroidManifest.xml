<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Shared capability dependencies declare biometric permissions, but Slice 1
         does not ship App Protection and must not request unused capabilities. -->
    <uses-permission
        android:name="android.permission.USE_BIOMETRIC"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.USE_FINGERPRINT"
        tools:node="remove" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_enpo_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_enpo_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnpoConnect">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
