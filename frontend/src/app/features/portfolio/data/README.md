# Portfolio migration data

`portfolio.source.ts` is a preserved copy of the original portfolio content and
is the migration source of truth for Phase 05.

Do not copy the legacy application's layout, navigation, global CSS, theme
engine, or build output into Bot Global Platform.

Later slices should normalize this content into the models under
`../models/` and render it through the Bot Global public shell and design
system.
